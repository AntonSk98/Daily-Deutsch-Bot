package com.ansk.development.learngermanwithansk98.service.filter;

/**
 * Filter that checks whether the incoming command is a known one
 * or checks whether one of the commands is being already processed.
 *
 * @author Anton Skripin
 */
public class CanFindCommandFilter implements IFilter {
    @Override
    public void filter(FilterParameters parameters) {

    }
}
